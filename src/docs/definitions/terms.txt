abilities, actions, adapters, add-ons, algorithms, animations, APIs, architecture, assets, attributes, behaviors, blueprints, buffers, buttons, caches, callbacks, canvases, channels, checkboxes, clusters, components, configs, containers, contexts, controls, cooldowns, data, datasets, defaults, definitions, dependencies, dialogs, dictionaries, displays, documents, effects, elements, entities, environments, events, features, fields, flags, flows, forms, frameworks, functions, handlers, heuristics, hitboxes, HUD, icons, identifiers, indexes, inputs, interfaces, items, jobs, keys, labels, layouts, layers, lifecycles, lists, loaders, logic, managers, manifests, mechanics, menus, messages, metadata, methods, metrics, migrations, modules, modes, nodes, notifications, objects, operations, options, overlays, packages, pages, panels, parameters, pipelines, popups, presets, processes, profiles, properties, protocols, queues, records, renderers, resources, responses, routes, rules, schemas, scripts, selectors, services, sessions, settings, shaders, spawners, sprites, states, stats, steps, storage, streams, structures, systems, tasks, templates, textures, thresholds, toggles, tooltips, transitions, triggers, UI, UX, utilities, variables, views, widgets, windows, workflows
